export interface Menu {
  //Agregando las propiedades min 22.26 parte 7
  //Deben coincidir con las propiedades que retorna la api (mayúsculas y minúsculas)
  idMenu: number;
  nombreMenu: string;
  icono: string;
  url: string;
}
