export interface Login {
  //Agregando las propiedades min 18.30 parte 7
  //Recibe las credenciales del usuario
  //Deben coincidir con las propiedades que retorna la api (mayúsculas y minúsculas)
  correo: string;
  clave: string;
}
